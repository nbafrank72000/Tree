<% if album.past %>
	<div class = "album">
		<div class = "album_past">

			<div class = "left-top">
				<%= link_to image_tag(album.user.headpic.url, alt: album.user.name, class: "headpic"), user_path(album.user_id) %>
			</div>
			<div class = "right-top">
				<div class = "datetime"><%= album.created_at.to_date.to_s %></div>
				<div class = "name"><%= album.name %></div>
			</div>
			<div class = "middle">
				<%= link_to image_tag(album.photos[0].picture_url), album %>
			</div>
			<div class = "bottom">
				<% if current_user?(album.user) %>
					<div class = "action">
						<%= link_to "Delete", album, method: :delete, data: {confirm: "You sure?"}, class: "btn btn-delete" %>
					</div>
					<div class = "action">
							<%= link_to "Past", change_past_now_albums_path(:id => album.id), data: {confirm: "Change it to Now?"}, class: "btn btn-past" %>
					</div>
				<% else %>
					<div class = "action">
							<div class = "btn btn-past">Past</div>
					</div>
				<% end %>
			</div>

		</div>
	</div>
<% else %>
	<div class = "album">
		<div class = "album_present">

			<div class = "left-top">
				<%= link_to image_tag(album.user.headpic.url, alt: album.user.name, class: "headpic"), user_path(album.user_id) %>
			</div>
			<div class = "right-top">
				<div class = "datetime"><%= album.created_at.to_date.to_s %></div>
				<div class = "name"><%= album.name %></div>
			</div>
			<div class = "middle">
				<%= link_to image_tag(album.photos[0].picture_url), album %>
			</div>
			<div class = "bottom">
				<% if current_user?(album.user) %>
					<div class = "action">
						<%= link_to "Delete", album, method: :delete, data: {confirm: "You sure?"}, class: "btn btn-delete" %>
					</div>
					<div class = "action">
							<%= link_to "Now", change_past_now_albums_path(:id => album.id), data: {confirm: "Change it to Past?"}, class: "btn btn-now" %>
					</div>
				<% else %>
					<div class = "action">
							<div class = "btn btn-now">Now</div>
					</div>
				<% end %>
			</div>

		</div>
	</div>
<% end %>